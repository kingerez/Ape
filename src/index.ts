import { loremIpsum, age, arrayOf, name, date, image, fromValues, color } from './types';

import { Ape } from './ApeBuilder';

export default Ape;
export {
  loremIpsum,
  age,
  arrayOf,
  name,
  date,
  image,
  fromValues,
  color,
};
