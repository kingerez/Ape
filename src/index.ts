import { loremIpsum, age, arrayOf, name, date, image, fromValues, color, email, url } from './types/index';

import { Ape } from './ApeBuilder';

export default Ape;
export {
  loremIpsum,
  age,
  arrayOf,
  name,
  date,
  image,
  fromValues,
  color,
  email,
  url,
};
